//---------------------------------------------------------------------------

#ifndef TDAColaPrioridadVectorH
#define TDAColaPrioridadVectorH
#include "TDAColaPuntero.h"
//---------------------------------------------------------------------------
#endif


const int MAX = 100;

class TDAColaPrioridadVector
{
      private:
              TDAColaPuntero VC[MAX];
              int VF[MAX];
              int colaAct;
              int cant;
      public:
              void crear();
              void poner(int e,int pri);
              void sacar(int &e);
              int frecuencia_prioridad(int frec,int pri);


};